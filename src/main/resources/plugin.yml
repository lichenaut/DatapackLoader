name: DatapackLoader
version: '${project.version}'
main: com.lichenaut.datapackloader.DatapackLoader
api-version: '1.14'
authors: [ lichenaut ]
description: Automatically loads datapacks into the server's main world.
load: STARTUP
commands:
  dl:
    description: "Base command for 'help' and 'import' commands."
  dltp:
    description: "Teleport to the center of any world in spectator mode."